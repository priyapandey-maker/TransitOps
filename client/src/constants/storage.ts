/**
 * LocalStorage keys used throughout the application to avoid magic strings.
 */
export const STORAGE_KEYS = {
  THEME: 'transitops-theme',
  TOKEN: 'transitops-token',
  USER: 'transitops-user',
} as const;
